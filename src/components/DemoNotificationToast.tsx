import React, { useEffect } from 'react';
import { Info, Lock, CheckCircle2, X } from 'lucide-react';

interface DemoNotificationToastProps {
  isVisible: boolean;
  onClose: () => void;
  message?: string;
}

export const DemoNotificationToast: React.FC<DemoNotificationToastProps> = () => {
  return null;
};
